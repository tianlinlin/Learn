# set virtual env
first you need install virtualenv pip
```sh
    pip install virtualenv
```
this is in your sys
# project virtual env
in your project folder call sh and load env
```sh
    python -m venv venv
```
the second venv is the name of the venv ,can use other name

# activate venv
in the terminal
```sh
venv\Scripts\activate
```
