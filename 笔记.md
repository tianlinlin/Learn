1.  print 标准化输出
    1.  %
        1.  formated_string = 'The area of circle with a radius %d is %.2f.' %(radius, area) # 2 表示小数点后的 2 位有效数字
    2.  str.format
        1.  print('{} / {} = {:.2f}'.format(a, b, a / b))
    3.  f-strings
        1.  字符串以f开头，在相应位置插入数字
        2.  print(f'{a} / {b} = {a / b:.2f}')
2.  切片语法
    1.  [起始:结束:步长](步长默认省略，起始也可以省略，左闭右开)
    2.  负步长由后往前
    3.  和数组一样
3.  字符串方法
    1.  字符串.字符串函数()
    2.  capitalize(): 将字符串中的第一个字符转换为大写字母
    3. count(): 返回字符串中子字符串的出现次数，count(子字符串，start=..，end=..)。start 是计数的起始索引，end 是计数的最后一个索引。
    4. endswith(): 判断字符串是否以特定的子字符串结尾，返回 True 或 False
    5. expandtabs(): 用空格替换制表符，默认制表符大小为 8。它接受制表符大小参数
       1. challenge = 'thirty\tdays\tof\tpython'
       2. print(challenge.expandtabs(10)) # 'thirty    days      of        python'
    6. find(): 返回子字符串第一次出现的索引，如果未找到则返回 -1
    7. rfind(): 返回子字符串最后一次出现的索引，如果未找到则返回 -1
    8. format()https://www.programiz.com/python-programming/methods/string/format
       1. {1:9.3f}
       2. In this case, 230.2346 is allotted a minimum of 9 places including the ".".
    9.  index(): 返回子字符串的最小索引，附加参数表示起始和结束索引（默认为 0，字符串长度为 - 1）。如果未找到子字符串，则会引发 valueError。
        1.  challenge = 'thirty days of python'
        2.  sub_string = 'da'
        3.  print(challenge.index(sub_string))  # 7
    10. rindex(): 返回子字符串的最大索引，附加参数表示起始和结束索引（默认为 0，字符串长度为 - 1）。如果未找到子字符串，则会引发 valueError。
        1.  challenge = 'thirty days of python'
        2.  sub_string = 'da'
        3.  print(challenge.rindex(sub_string))  # 8
    11. isalnum(): 判断字符串字符是否都是字母数字字符
        1.  challenge = 'thirty days of python'
        2.  print(challenge.isalnum()) # False, 空格不是字母字符
    12. isalpha(): 判断字符串字符是否都是字母字符 (a-z and A-Z)
    13. isdecimal(): 判断符串中的所有字符是否都是十进制 (0-9)，含有空格false
    14. isdigit(): 判断字符串中的所有字符是否都是数字（0-9 和一些其他表示数字的 Unicode 字符）
        1.  challenge = '\u00B2'
        2.  print(challenge.isdigit())   # True
    15. isnumeric(): 判断字符串中的所有字符是否都是数字或与数字相关（就像 isdigit()，只是接受更多符号，如 ½）
    16. isidentifier(): 判断有效的标识符 - 检查字符串是否是有效的变量名
    17. islower(): 判断字符串中的所有字母是否都是小写
    18. isupper(): 判断字符串中的所有字母是否都是大写
    19. join(): 返回连接后的字符串
        1.  
        ```py
        web_tech = ['HTML', 'CSS', 'JavaScript', 'React']
        result = ' '.join(web_tech)
        print(result) # 'HTML CSS JavaScript React'
        ```
    20. strip(): 删除字符串开头和结尾的所有给定字符
        1.  challenge = 'thirty days of pythoonnn'
        2.  print(challenge.strip('noth')) # 'irty days of py'
    21. replace(): 用给定的字符串替换子字符串
        1.  print(challenge.replace('python', 'coding')) # 'thirty days of coding'
    22. split(): 使用给定的字符串或空格作为分隔符来拆分字符串
        1.  print(challenge.split()) # ['thirty', 'days', 'of', 'python']
        2.  challenge = 'thirty, days, of, python'
        3.  print(challenge.split(', ')) # ['thirty', 'days', 'of', 'python']
    23. title(): 返回标题大小写的字符串
    24. swapcase(): 将所有大写字符转换为小写字符，将所有小写字符转换为大写字符
    25. startswith(): 判断字符串是否以指定字符串开头
4.  列表方法
    1.  first_item, second_item, third_item, *rest = lst
        1.  *这里的*表示取列表第三个元素之后的所有值
    2.  列表切分
        1.  reverse_fruits = fruits[::-1]
        2.  orange_and_mango = fruits[1:3] # 不包含第一项
    3.  列表添加项（尾部插入）
        1.  lst = list()
        2.  lst.append(item)
    4.  列表添加项（中间插入）
        1.  lst = list()
        2.  lst.insert(index, item)
        3.  表示在index号元素后面插入item
    5.  列表移除项（指定项）
        1.  lst = ['item1', 'item2']
        2.  lst.remove(item)
    6.  列表移除项（指定index）
        1.  lst.pop(index)
        2.  未指定默认最后一项
    7.  del关键字
        1.  del lst[index] # 只删除一项
        2.  del lst        # 删除整个列表
    8.  清空列表
        1.  lst.clear()
    9.  列表复制
        1.  可以通过将其重新分配给新变量来复制列表: list2 = list1。现在，list2 是 list1 的引用，我们对 list2 进行的任何更改也将修改原始的 list1。但是有很多时候我们不想修改原始的列表，而是想要一个不同的副本。为了避免这个问题，我们使用 *copy()*。
        2. lst_copy = lst.copy()
    10. 连接列表
       1.  list3 = list1 + list2 有顺序
       2.  list1.extend(list2) 有顺序，list1 在前
    11. 统计列表项
        1.  lst.count(item)
        2.  使用 *count()* 方法返回列表中指定项出现的次数:
    12. 查找列表项
        1.  *index()* 方法返回列表中项的索引:返回第一次出现item在列表的索引
    13. 列表翻转
        1.  使用 *reverse()* 方法反转列表的顺序。
        2.  lst.reverse()
    14. 列表排序
        1.  要对列表进行排序，我们可以使用 *sort()* 方法或内置函数 *sorted()*。*sort()* 方法将列表项按升序重新排序并修改原始列表。如果 *sort()* 方法的 reverse 参数为 true，则会按降序排列列表。
        2.  fruits.sort(reverse=True)
        3.  sorted(): 不会修改原始列表，而是返回一个新列表（函数）
            1.  fruits = sorted(fruits,reverse=True)
5.  元组方法
    1.  创建空元组
        1. empty_tuple = ()
        2. empty_tuple = tuple()
     2. tpl = ('item1', 'item2','item3')
     3. 计算长度
        1. len(tuple)
     4. 元组切片
        1. 我们可以通过指定开始和结束的索引范围来切出子元组，返回值是一个包含指定项的新元组。
        2. 跟列表一模一样
     5. 元组更改列表
        1. lst = list(tpl)
     6. 检索项
        1. in
        2. 'item2' in tpl 
     7. 连接元组
        1. tpl3 = tpl1 + tpl2
     8. 删除元组
        1. del tpl1
6. 集合方法
   1. 创建空集合
      1. st = set()
   2. st = {'item1', 'item2', 'item3', 'item4'}
   3. 获取长度
      1. len()
   4. 访问集合项目
   5. 检查项
      1. item in set
   6. 添加元素
      1. st.add('item5')
   7. 添加多个元素
      1. st.update(['item5','item6','item7'])
      2. st.update(list)
      3. st.update(tuple)
      4. st.update(set)
   8. 移除元素
      1. st.remove('item2')，不存在会报错
      2. st.discard('item2')，不存在无事发生
      3. fruits.pop()  # 从集合中移除一个随机元素，返回值为该随机元素
   9. 清除集合所有项目
      1.  st.clear()
   10. 删除集合
       1. del st
   11. 我们可以将列表转换为集合，也可以将集合转换为列表。将列表转换为集合会去除重复项，只保留唯一项。
       1.  st = set(lst) 
       2.  顺序是随机的，因为集合在一般情况下是无序的
   12. 合并集合
       1.  st3 = st1.union(st2)
       2.  st1.update(st2) # st2 的内容被添加到 st1 中
   13. 查找交集
       1.  交集返回两个集合中都存在的项的集合
       2.  python = {'p', 'y', 't', 'h', 'o', 'n'}
       3.  dragon = {'d', 'r', 'a', 'g', 'o', 'n'}
       4.  python.intersection(dragon)     # {'o', 'n'}
   14. 检查子集超集
       1.  st1 = {'item1', 'item2', 'item3', 'item4'}
       2.  st2 = {'item2', 'item3'}
       3.  st2.issubset(st1) # True
       4.  st1.issuperset(st2) # True
   15. 检查两个集合之间的差异（类似补集）
       1.  st1.difference(st2)
       2. st1\st2（集合1-集合2，只在1不在2的元素）
   16. 对称差异
       1.  它返回两个集合之间的对称差异。它意味着它返回一个包含两个集合中所有项的集合，除了同时出现在两个集合中的项，数学上：(A\B) ∪ (B\A)
   17. 检测是否没有交集
       1.  st2.isdisjoint(st1)
       2.  没有交集返回TRUE
7.  字典方法
    1.  创建字典
        1.  空：dct = {}
    2.  字典写法
        1.  dct = {'key1':'value1', 'key2':'value2', 'key3':'value3', 'key4':'value4'}
    3.  键值对的数量
        1.  len(dct)  
    4.  访问字典项
        1.  dct['key1']
        2.  get 方法
            1.  dct.get('key1')
            2.  这个方法的好处是访问字典中不存在的键的时候会出现none而不是报错
            3.  dct.get('key',默认值)
                1.  如果我们想要对一个列表里出现的每一个数进行计数，我们就可以用迭代器遍历列表，用字典存储计数值，用get方法找出字典里已经计数过的值
    5.  添加新的键值
        1.  dct['key5'] = 'value5'
        2.  如果key1对应的数据类型是列表，则dct['key1']的数据类型是列表，适用列表的语法
    6.  修改字典的项目
        1.  dct['key1'] = 'value-one'
    7.  检查字典中的项
        1.  用in语法，返回布尔值
    8.  删除字典键值对
        1.  字典存储键值的方式类似与一个栈，因此我们可以用_.pop_语法来删除其中一个键值
            1.  dct.pop('key1') # 删除 key1 项目
        2.  也可以用.popitem语法来删除最后一个项目_.popitem_
            1.  dct.popitem()
        3.  也可以用函数语法_del_来删除
            1.  del dct['key2']
    9.  将字典变成由元祖组成的列表_items_
        1. dct = {'key1':'value1', 'key2':'value2', 'key3':'value3', 'key4':'value4'}
        2. print(dct.items()) 
        3. dict_items([('key1', 'value1'), ('key2', 'value2'), ('key3', 'value3'), ('key4', 'value4')])
    10. 清空与删除字典
        1.  dct.clear()
        2.  del dct
    11. 复制字典
        1.  dct_copy = dct.copy()
    12. 获取键列表
        1.  keys = dct.keys()
    13. 获取值列表
        1.  values = dct.values()
8. 循环
   1. while循环
      1. 和c一样，有break和continue语法
      2. while condition:
      3. code goes here
      4. else:
      5. code goes here
   2. for循环
      1. 更简单遍历列表元组集合字典字符串
      2. 列表、元组、字符串类似如下
         1. for iterator in lst:
         2. code goes here
      3. 字典遍历
         1. for key in person: 这个方法只遍历键
         2. for key, value in person.items(): 这个方法同时遍历键、和值
      4. for else 语法也适用
   3. range()函数
      1. 函数用于生成一个序列的数字。_range(start, end, step)_函数接受三个参数：起始值、结束值和步长。默认情况下，起始值是0，步长是1。这个函数需要至少一个参数。（结束值end）
9. 函数：
   1.  def function_name():
   2.  函数返回值
       1.  一个函数没有 return 语句，那么函数的返回值为 None
   3.  传递参数：和c一样，但是不用声明类型
       1.  可以传递默认参数
           1.  function_name(param = value):
       2.  可以传入不定数量的参数
           1.  def function_name(*args):
10. 模块
    1.  导入模块文件
        1.  _import_
            1.  import mymodule
            2.  mymodule.function
        2.  _from_import_
            1.  用来导入模块文件中的函数或多个函数
                1.  from mymodule import generate_full_name, sum_two_nums, person, gravity
        3.  _as_用作重命名模块名称
            1.  from mymodule import generate_full_name as fullname, sum_two_nums as total, person as p, gravity as g
    2.  导入内置模块
        1.  常见模块
            1.  _math_、_datetime_、_os_、_sys_、_random_、_statistics_、_collections_、_json_、_re_
        2.  OS 模块
            1.  模块可以自动执行许多操作系统任务。Python 中的 OS 模块提供了创建、更改当前工作目录和删除目录（文件夹）、获取其内容、更改和识别当前目录的函数。
            2.  os.listdir(path='.')
                1.  返回指定目录下文件的目录名列表
            3.  os.remove(path)
                1.  删除指定地址的文件
            4.  now_path = os.path.abspath(__file__)
                1.  获取脚本文件所在文件夹的绝对路径
            5.  folder_path = os.path.dirname(now_path)
                1.  now_path 所在的文件夹的绝对路径
            6.  os.path.join(script_dir, 'file_name.txt')
                1.  将两个路径拼接
11. 列表推导式
    1.  [i for i in iterable if 表达式]
        1.  表示列表第i项是iterable遍历第i项
        2.  多重for循环： [number for row in list_of_lists for number in row]
            1.  结算顺序：
                1.  先跑一层外层，然后遍历内层
        3.  与_if_表达式连用
            1.  [i for i in range(21) if i % 2 == 0 and i > 0]
12. _lambda_    
    1.  使用 _lambda_ 关键字，然后是一个或多个参数，再然后是一个表达式
    2.  sorted函数应用lambda
        1.  sorted(data, key=lambda x: (-x[1], x[0]))
        2.  原始数据 → 应用 key 函数 → 得到键 → 根据键排序 → 返回原始元素（按键的顺序）
        3.  这里lambda返回一个元组，作为key来作为比较的依据
13. 高阶函数
    1.  函数可以将函数作为参数传入函数内
        1.  ![alt text](image.png)
        2.  也可以以根据参数返回不同的函数，达到一个函数在不同情况使用对应函数的目的
14. 闭包
    1.  Python 允许嵌套函数访问外部封闭函数的作用域。 这称为闭包。 
15. 装饰器
    1.  装饰器是一种设计模式，允许用户在不修改对象结构的情况下为其添加新功能。装饰器通常在你想要装饰的函数定义之前调用。
    2.  语法糖:
        1.  @:应用装饰器
            1.  多个@从上到下包装，也就是说从下到上结算函数
16. map 函数
    1.  map() 适合将同一个函数批量应用到序列的每个元素，尤其当你已有函数、追求函数式编程风格或需要惰性处理大数据集时。但在普通情况下，列表推导式或生成器表达式往往是更推荐的选择。
    2.  map(function, iterable)
    3.  function 可以使用lambda简化
    4.  返回值
        1.  一个 map 对象（迭代器），惰性求值，仅在需要时（如转换为列表或遍历）才计算每个元素。
        2.  也就是说，直接返回map函数是返回map函数的地址，而不是值，只有在需要他输出列表之类的时候才进行计算
    5.  可以进行多个iterable的值进行计算
17. Filter 函数
    1.  filter()函数调用指定函数，该函数对指定的可迭代对象（列表）中的每个项目返回布尔值。它过滤出满足过滤条件（true）的项目。
    2.  实际上，我们也可以用列表表达式来求解单个
    3.  惰性求值
18. reduce 函数
    1.  *reduce()*函数定义在 functools 模块中，我们需要从这个模块中导入它。像 map 和 filter 一样，它接收两个参数，一个函数和一个可迭代对象。然而，它不返回另一个可迭代对象，而是返回一个单一的值。
    2.  语法
        1.  reduce(function, iterable[, initializer])
        2.  本质
            1.  reduce(f, [a, b, c, d])
                1.  f(f(f(a, b), c), d)
        3.  initializer（可选）：初始值，如果提供，则作为第一次调用的第一个参数；如果可迭代对象为空且提供了初始值，则直接返回该初始值。
19. datatime 模块
    1.  导入
        1.  import datetime
        2.  ['MAXYEAR', 'MINYEAR', '__builtins__', '__cached__', '__doc__', '__file__', '__loader__', '__name__', '__package__', '__spec__', 'date', 'datetime', 'datetime_CAPI', 'sys', 'time', 'timedelta', 'timezone', 'tzinfo']
    2.  datetime 函数
        1.  返回 类似 2021-07-08 07:34:46.549883
        2.  now = datetime.now()，数据类型是datetime
        3.  month = now.month minute = now.minute second = now.second可以访问now里面的秒、分、时等
        4.  也可以自己生成一个时间new_year = datetime(2020, 1, 1)
        5.  也可以生成时间戳
            1.  timestamp = now.timestamp()
            2.  时间戳或Unix时间戳是从1970年1月1日UTC开始经过的秒数。
        6.  strftime 方法
            1.  字符串方法
                1.  print(f'{day}/{month}/{year}, {hour}:{minute}')  # 1/1/2020, 0:0
            2.  strftime方法
                1.  time_one = now.strftime("%m/%d/%Y, %H:%M:%S")
                2.  time_two = now.strftime("%d/%m/%Y, %H:%M:%S")
                3.  类似上面的写法
    3.  date 函数
            1.  date函数只还回年月日
                1.  可以接受返回当前日期
                    1.  today = date.today()
                2.  也可以返回特定日期
                    1.  d = date(2020, 1, 1)
                3.  可以想datetime一样单独访问年月日
    4.  time 函数
        1.  time(hour, minute, second, microsecond)
        2.  可以返回当前时间，但是是以时间戳的形式返回
            1.  now_ts = time.time()
            2.  也可以转化为可读形式
                1.  print(time.ctime())                     
                    1.   Sat Apr 11 14:30:25 2026
                2.  t = time.localtime() 
                    1.  print(time.strftime("%Y-%m-%d %H:%M:%S", t))   
                        1.  2026-04-11 14:30:25
    5.  利用datetime计算时间点差
        1.  t1 = datetime(year = 2019, month = 12, day = 5, hour = 0, minute = 59, second = 0)
        2.  t2 = datetime(year = 2020, month = 1, day = 1, hour = 0, minute = 0, second = 0)
        3.  diff = t2 - t1
            1.  注意，diff也是datetime数据类型
    6.  利用timedelta
        1.  t1 = timedelta(weeks=12, days=10, hours=4, seconds=20)
        2.  t2 = timedelta(days=7, hours=5, minutes=3, seconds=30)
        3.  t3 = t1 - t2
        4.  print("t3 =", t3)
            1.  t3 = 86 days, 22:56:50
            2.  注意，t3的数据类型也是timedelta，与前面的区别就是输出的时间格式不一样
20. 解包 unpacking
    1.  * （c中，*一定程度上表示解指针，这里就是解包，可以解列表解元组、集合）
    2.  解包（unpacking） 允许将一个可迭代对象（如列表）的元素按位置依次赋给多个变量
    3.  例如
        1.  args = [2, 7]
        2.  *args = 2,7
    4.  扩展解包（extended unpacking），星号 * 起到了“收集剩余元素”的作用
        1.  countries = ['Finland', 'Sweden', 'Norway', 'Denmark', 'Iceland']
        2.  fin, sw, nor, *rest = countries
    5.  解包字典
        1.  ** 字典解包（dictionary unpacking）
        2.  def unpacking_person_info(name, country, city, age):
        3.  return f'{name}住在{country}的{city}。他{age}岁。'
        4.  dct = {'name':'Asabeneh', 'country':'Finland', 'city':'Helsinki', 'age':250}
        5.  print(unpacking_person_info(**dct))
            1.  原理就是先解包把key解出来，然后解包解出key里面的value
21. 打包 packing
    1.  有时候我们不知道需要向Python函数传递多少个参数。我们可以使用打包方法让我们的函数接受无限数量或任意数量的参数。
    2.  def sum_all(*args):此时，我们就可以传入一个任意长度的列表了
    3.  打包字典
        1.  def packing_person_info(**kwargs):
22. 展开
    1.  lst_one = [1, 2, 3]
    2.  lst_two = [4, 5, 6, 7]
    3.  lst = [0, *lst_one, *lst_two]
23. enumerate 函数
    1.  enumerate(list)
    2.  遍历返回index item
24. Zip
    1.  返回一个迭代器，每次产生一个元组，元组中的第 i 个元素来自每个可迭代对象的第 i 个元素。
    2.  当最短的输入可迭代对象耗尽时停止（不会填充缺失值）。
    3.  当需要指定严格长度
        1.  strict=True
            1.  若需要zip的两个列表长度不一样，会返回ValueError
    4.  unzip
        1.  lst1, lst2... = zip(*zipped)
    5.  zip_longest
        1.  会填充缺失值而不是直接停止
            1.  list(zip_longest([1,2], [3], fillvalue=0))  # [(1,3), (2,0)]
    6.  可以用来转置矩阵
        1.  transposed = list(zip(*matrix))
25. 正则表达式
    1.  导入模块
        1.  import re
    2.  re.match()
        1.  从字符串的 起始位置 开始，检查是否匹配某个正则表达式模式
        2.  re.match(substring, string, re.I)
            1.  substring是一个字符串或模式，string是我们查找模式的文本，re.I是忽略大小写
        3.  返回值
            1.  当 re.match()（或 re.search()、re.fullmatch()）匹配成功时，返回的是一个 re.Match 类的实例。这个对象内部存储了：
                1.  原始字符串（string）
                2.  正则表达式模式编译后的内部表示
                3.  匹配的起始和结束位置（索引）
                4.  所有捕获组（capturing groups）的边界信息
            2.  匹配失败：返回 None
            3.  ps：py里面每一个有value的值都可以视为true
        4.  括号捕获,这成为捕获组
            1.  m = re.match(r'(\d{4})-(\d{2})-(\d{2})', '2026-04-11')
            2.  上面正则表达式有3个括号（三个捕获组），如果我们想要引用第一个括号，可以使用m.group(1)
            3.  如果要返回全部匹配的内容，可以使用m.group(0)
    3.  re.search()
        1.  扫描整个字符串，返回第一个匹配的位置
        2.  同样有捕捉组的概念、且与match相似
        3.  语法
            1.  re.search(pattern, string, flags=0)
                1.  flag可为re.I 即忽略大小写
    4.  re.findall()
        1.  语法
            1.  re.findall(pattern, string, flags=0)
        2.  re.findall 返回一个 列表（list），内容取决于正则表达式中是否包含捕获组（括号分组）。
            1.  如果没有捕获组，返回一个没有元组的列表
            2.  如果有捕获组，返回一个元素为元组的列表，元组内的元素个数为捕获组的个数
            3.  如果未匹配，返回none
    5.  正则表达式书写
        1.  []表示一组字符同时参与匹配
            1.  [Aa]    表示大小写均可
            2.  [a-zA-Z0-9]
        2.  \
            1.  \d表示数字\w表示字母
            2.  要查找原始字符里面的\，要输入r.'\\'
        3.  .
            1.  表示匹配任意字符
        4.  *
            1.  表示零次或多次
            2.  比如.*，长度就是1+ .+长度就是2+
        5.  ?
            1.  零次或一次
                1.  当 ? 紧跟在某个元素（字符、字符类、分组等）之后，且前面没有其他量词时，它表示该元素可选，即出现 0 次或 1 次。
            2.  改变匹配模式
                1.  当 ? 紧跟在另一个量词（*、+、?、{m,n}）之后时，它将那个量词的匹配模式从贪婪切换为非贪婪（尽可能少匹配）。
            3.  分组扩展
                1.  ![alt text](image-1.png)
        6.  {}
            1.  指定模式的长度
        7.  ^
            1.  以什么为开始
            2.  否定
                1.  [^a-z]表示非小写字母
        8.  eg1:r'(\w+)@\w+\.\w+'
            1.  r'...'
                1.  Python 的原始字符串前缀。防止反斜杠 \ 被当成普通转义符，确保正则引擎正确解释 \w 和 \.。
            2.  (\w+)
                1.  group(1)
                2.  \w 匹配一个字符、数字、_。
                3.  + 表示贪婪匹配，匹配至少一个‘w’，但是如果文本没有‘w’，则匹配失败
            3.  \. 
                1.  \ ，在这里转义‘ . ’，因为在正则表达式里，. 表示匹配任意字符
        9.  eg2：r'(\d{4})-(\d{2})-(\d{2})'
            1.  \d{4} \d 表示匹配数字，{4}表示匹配四位数字
        10. eg3;
            1.  r'(?<!^)(?=[A-Z])'
                1.  零宽断言
                2.  (?= ...) 正向前瞻断言
                    1.  匹配一个位置，该位置后面的字符必须符合括号内的模式。不消耗字符，仅做判断。
                3.  (?<! ...)负向后顾断言
                    1.  匹配一个位置，该位置前面的字符不能符合括号内的模式。
                4.  (?=[A-Z])
                    1.  匹配一个位置，该位置的后一个字符是大写字符
                5.  (?<!^)(?=[A-Z])
                    1.  匹配一个位置，该位置的前一个字符不为字符串开头符'^'，后一个字符为大写字母
    6.  re.sub()
        1.  语法
            1.  re.sub(pattern, repl, string, count=0, flags=0)
            2.  count 表示匹配最大次数，默认为0，即匹配全部
            3.  repl 
                1.  替换内容。可以是字符串（支持反向引用），也可以是函数（接收一个 Match 对象，返回替换后的字符串）
                2.  \i = \g<i> ，表示引用前面第i个捕获组
                3.  当 repl 是一个可调用对象（函数或 lambda）时，它会在每次匹配时被调用，并接收一个 Match 对象作为唯一参数。函数返回的字符串将作为本次匹配的替换内容。
                    1.  result = re.sub(r'(\w)(\d)', lambda m: m.group(1).upper() + m.group(2), text)
        2.  re.subn
            1.  同时返回替换次数，返回值是一个元组
    7.  re.split()
        1.  re.split(pattern, string, maxsplit=0, flags=0)
        2.  当 pattern 中包含捕获组 (...) 时，分割后的列表会交错插入捕获组匹配到的内容（即分隔符本身）。
        3.  过滤空字符
            1.  [part for part in re.split(r',', text) if part]
26. 文件处理
    1.  打开文件
        1.  open('filename', mode)
            1.  r：只读
            2.  a：追加
            3.  w：只写
            4.  x：创建
            5.  t：文本模式
            6.  b：二进制
            7.  open函数默认读取
        2.  f = open('./files/reading_file_example.txt')称为已经打开的文件
            1.  read方法处理f
                1.  将整个文本作为字符串读取。如果我们想限制想要读取的字符数，可以通过向 *read(number)* 方法传递int值来限制。
            2.  readline方法处理f
                1.  只读取一行
            3.  readlines
                1.  读取多行，并且返回一个行列表
        3.  在我们打开文件之后，需要用close方法关闭文件f.close()
        4.  用with方法打开文件，会自动在调用完文件后关闭文件
            1.  with open('./files/reading_file_example.txt') as f:
    2.  写入文件
        1.  open时要注意加入a、w
            1.  f.write('context')
    3.  删除文件
        1.  需要引入os系统模块
            1.  import os
            2.  os.remove('./files/example.txt')
        2.  如果文件不存在，会报错，这时候要调用os中判断目录是否存在的函数来验证
            1.  if os.path.exists('./files/example.txt'):  
    4.  文件类型
        1.  json
            1.  JSON代表JavaScript对象表示法。实际上，它是一个字符串化的JavaScript对象或Python字典。
            2.  将json转化为字典
                1.  导入json模块，使用loads方法
                    1.  import json 
                    2.  person_dct = json.loads(person_json)
            3.  将字典转化为json
                1.  使用dumps方法
                    1.  json_string = json.dumps(person_dct, **options)
                        1.  option常见处理
                            1.  indent
                                1.  indent = 4表示每一个键换行打印，且前面有四个空格
                                2.  indent = ' '表示自定义格式化类型
                            2.  sort_keys =  true
                                1.  按键的首字母排序
                            3.  ensure_ascii=False 
                                1.  JSON 字符串中的非 ASCII 字符将原样保留，以 UTF-8 编码的原生字符形式呈现。
            4.  我们也可以直接创建json文件
                1.  with open('./files/json_example.json', 'w', encoding='utf-8') as f:
                    1.  json写入函数 dump
                        1.  json.dump(obj, fp, ensure_ascii=False, indent=4)
                            1.  obj：序列化py对象
                            2.  fp写入模式打开的文件
        2.  csv
            1.  打开
                1.  with open('./files/csv_example.csv', 'w', encoding='UTF8', newline='') 
                2.  读取
                    1.  csv.reader 将每一行作为列表返回，适合直接按位置处理列。
                    2.  csv.DictReader 将第一行作为字段名，每一行作为字典返回，代码可读性更高。
                3.  写入
                    1.  writer = csv.writer(f)
                        1.  生成将列表写入文件的包装器
                    2.  writer.writerows(data_list) 
                        1.  传入列表，一次写入多行
                    3.  writer = csv.DictWriter(file, fieldnames=list_key)
                        1.  生成将字典写入csv的文件的包装器
                            1.  后面的fieldnames是一个包含键的列表，他指示包装器以怎样的键顺序生成csv文件
                    4.  writer.writeheader()  
                        1.  写入表头（list_key）这里py会默认表头
                    5.  writer.writerows(data_dict)
                        1.  写入数据
        3.  xlsx
        4.  xml
            1.  XML是一种元标记语言，非常类似于HTML。在XML中，我们可以使用自己的标签，从而使其更加灵活。我们使用XML主要是为了结构化数据。在Python中有少量的XML库。在本部分中，我们将使用xml.etree.ElementTree模块。
            2.  xml.etree.ElementTree
                1.  import xml.etree.ElementTree as ET
27. os 基础
    1.  在这一章节，我们简单介绍几个os麾下函数
        1.  首先，介绍一些简写
            1.  dir：目录
        2.  如果想要获取脚本所在的绝对路径，可以使用如下代码
            1.  file_path = os.path.abspath(_file_)
        3.  如果想要获取脚本所在文件夹的路径
            1.  folder_path = os.path.dirname(file_path)
        4.  如果想要获取某个文件夹下的所有目录名(不含后缀)
            1.  name_list = os.listdir()
        5.  如果想要获取某个文件夹下带有xx后缀的文件名列表
            1.  列表表达式法
                1.  files = [file for file in os.listdir(fold_path) if file,endswith('.xx')]
                2.  在这里，使用了函数_endswith()_
        6.  如果想要遍历文件夹
            1.  os.walk()
                1.  返回值：(root, dirs, files)
                    1.  root表示迭代器当前所在文件夹的路径
                    2.  dirs表示迭代器所在文件夹下其他文件夹的列表名称（不是路径）
                    3.  files表示迭代器当前所在文件夹下文件名称的列表（不是路径）
                2.  这时，如果想要遍历一个文件夹下的所有文件
                    1.  for root, dirs, files in os.walk(folder_path)；
                        1.  注意，这个遍历输出的文件列表只包含该root下的文件，如果想要获取总表，应该在循环外建立一个列表
                        2.  获取地址：path = os.path.join(root,file),file是for file in files 的迭代器
28. 类
    1.  为什么要使用类？
        1.  类本质上是创造了一种容器模板在这个模板中，声明了一个每一个物体对应的属性
        2.  之后，我们可以通过调用类内层函数来实现对这个物体的处理，也可以用访问类中某个属性的方式在外部进行处理
    2.  类声明
        1.  class Person:
    3.  类初始化
        1.  采用构造函数
        2.  def __init__ (self, name):
            1.  这里。self指示当前类实例的引用。
            2.  name就是传入的一个参数
                1.  我们可以用self.name = name为类中name参数赋值
    4.  继承
        1.  class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        pass  # 占位，由子类实现